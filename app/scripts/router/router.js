define([
	'jquery',
	'backbone',
	'views/search',
	'views/albumsearch',
	'views/artistsearch'
], function($, Backbone, SearchView, AlbumSearchView, ArtistSearchView) {
	var Router = Backbone.Router.extend({

	});

	return Router;

});
